# 03 — Decision Log

Every choice below is FINAL for the hackathon build. Do not re-litigate mid-sprint. If a new decision is genuinely needed, add it here with a date and reason, then proceed.

| Area | Decision | Reason |
|---|---|---|
| LLM | `gemini-2.5-flash` for all agents | Fast + cheap for iteration; upgrade to Pro only for the final Briefing Agent if quality needs it |
| Agent framework | Google ADK, `SequentialAgent` orchestration pattern | Matches hackathon's mandatory-stack requirement; sequential fits our linear Triage → Bed-Match → (Routing → Briefing) flow |
| Cloud deployment | Cloud Run (FastAPI app) | Satisfies mandatory Google Cloud service requirement, simple container deploy |
| Audit storage | Firestore | Serverless, minimal setup, doubles as the "auditability" story from the original pitch |
| Hospital data | **Google Places API** (real names/locations) | All-Google stack story for the hackathon; requires billing-enabled GCP project |
| Routing/ETA | **Google Routes API** | Same reasoning — keeps entire stack on Google, same GCP project/billing as Places |
| Bed/specialty data | Simulated, generated by `scripts/seed_mock_data.py` | Real EHR bed data requires private hospital integration, out of scope for a hackathon |
| Triage scoring | NEWS2 (public clinical standard), implemented as a plain Python function | Grounds Triage Agent's reasoning in a real formula instead of an invented score |
| Demo city | **[FILL IN — e.g. "Ludhiana, Punjab"]** | Needs real hospital density for Places API to return good results |
| Front end | Streamlit | Fastest to build a scripted, reliable demo UI |
| Demo input mode | Preset scenarios only, no free text | Reliability over flexibility for a live/recorded demo |
| Stretch agents | Routing Agent + Briefing Agent are OPTIONAL, only built if Day 1–2 finish early | See `07-scope-lock.md` |

## Open items (fill before Day 1 starts)

- [ ] **Demo city** — pick one and write it into this table and into `scripts/fetch_hospitals.py`
- [ ] **GCP project name/ID** — create it, note it in `.env.example`
- [ ] **Team member responsible for each file** (optional, if more than one person is building)
