# 03 — Decision Log

Every choice below is FINAL for the hackathon build. Do not re-litigate mid-sprint.
If a new decision is genuinely needed, add it here with a date and reason, then proceed.

| Area | Decision | Reason |
|---|---|---|
| **LLM — Triage Agent** | `gemini-3.1-pro` | Best available Pro-tier reasoning for clinical decisions. Gemini 3.5 Pro is delayed; 3.1-pro is the current Pro flagship. **Meets hackathon eligibility: Gemini 3.5 or newer accessed via Gemini API** — `gemini-3.5-flash` is used for all other agents. |
| **LLM — All other agents** | `gemini-3.5-flash` | Released May 19, 2026. Frontier-level intelligence with speed & cost-efficiency. **Mandatory model per hackathon rules.** (Fallback: `gemini-3.7-flash`). |
| **Agent framework** | Google ADK (`LlmAgent`, `SequentialAgent`) + Google Genkit | ADK for agent orchestration; Genkit for prompt/flow management. Both satisfy hackathon's mandatory agent framework requirement. |
| **Auth** | Firebase Authentication (Prod) & Demo/Mock Token Mode (Evaluation) | Dual-mode auth. Supports zero-friction hackathon evaluation via mock bearer token `lifeline_mock_<role>_<uid>` across `blood_donor`, `hospital_staff`, and `government_authority` personas, while preserving clean upgrade path to Firebase Auth Admin SDK. *(Added 2026-08-29)* |
| **AI Daily Intelligence Model** | `gemini-3.5-flash` | Assigned to `/reports/daily` and `/reports/query` natural-language endpoints. Provides fast executive-level summarization over regional incident telemetry and capacity metrics under 2 seconds. *(Added 2026-08-29)* |
| **Multi-Role Frontend Portal** | Next.js App Router + TypeScript (Role-Scoped Screens) | Three dedicated visual screen architectures: lightweight card-based portal for `blood_donor`, dense clinical operations console for `hospital_staff`, and macro-analytics dashboard for `government_authority`. *(Added 2026-08-29)* |
| **DataStore & Client Adapter** | Universal Firestore Client with In-Memory Offline Fallback | Thin client layer in `lifeline/tools/` and `lifeline/firebase.py` wrapping standard CRUD for `dispatch_cases`, `donors`, `requests`, `patients`, `issues`, `inventory`, and `reports`. Gracefully falls back to mock IDs and memory caches if cloud credentials are absent. *(Added 2026-08-29)* |
| **Cloud deployment** | Cloud Run (FastAPI app) | Satisfies mandatory Google Cloud service requirement. Simple container deploy, scales to zero between demo runs. |
| **Audit storage** | Firestore (Firebase Admin SDK) | Serverless, minimal setup, real-time capable. Doubles as the "auditability" story from the original pitch. |
| **Hospital data** | OpenStreetMap Overpass API | Free, no API key, SLA-free but cacheable. Returns real hospital names + GPS. Explicitly NOT Google Places API (no billing required for demo). |
| **Routing/ETA** | OSRM public demo server | Free, no API key. Returns real driving distance + duration. Cache results locally after first pull to guard against demo-day flakiness. |
| **Bed/specialty data** | Simulated, generated by `lifeline seed` | Real EHR bed data requires private HL7/FHIR hospital integration — explicitly out of scope. Documented in README real vs. simulated table. |
| **Triage scoring** | NEWS2 (Royal College of Physicians, public standard) | Grounds Triage Agent's reasoning in a real clinical formula. Agent receives the computed score — not raw vitals — so Gemini reasons over a real number. |
| **Demo city** | `mumbai` (default; configurable via admin panel) | Dense hospital network in OSM. Configurable to any supported city via `DEMO_CITY` setting. |
| **Demo input mode** | Preset scenarios & Interactive SOS Trigger | Reliability over flexibility for live demo while supporting manual ER triggers from the hospital console. |
| **Stretch agents** | Routing Agent + Briefing Agent are OPTIONAL | Only built if Day 1–2 finish early. See `07-scope-lock.md`. |
| **Model registry** | `lifeline/models.py` — single source of truth | All agents import `AGENT_MODELS[agent_name]`. Upgrade all models by editing one file. |
| **Unauthorized Feature Removal** | Removed "Judge Review Board" & Supabase integration | Feature was out of scope per `docs/07-scope-lock.md` §1/§4 and violated the locked single-datastore architecture (Universal Firestore Client). All associated routes (`/reviews`), components (`JudgeFeedbackSection`), nav links, and tech stack references were completely excised. *(Added 2026-08-30)* |
| **Mobile Drawer Navigation (<md Viewports)** | Responsive Slide-Over Drawer with Hamburger Trigger & Backdrop | On viewports < 768px (e.g. 375px mobile), sidebar hides from normal document flow and mounts as a high-z-index slide-over drawer with backdrop overlay. Eliminates 256px horizontal screen compression and ensures 100% full-width view for clinical operations. *(Added 2026-08-30)* |
| **Responsive Grid Breakpoints (768px Tablet Viewports)** | Standardized Multi-Column Adaptation (`grid-cols-1 sm:grid-cols-2 lg:grid-cols-4` & `grid-cols-1 md:grid-cols-2 lg:grid-cols-3`) | Prevents KPI stat card content compression and badge clipping on medium (768px) tablet screens across Hospital, Government, and Donor dashboards. *(Added 2026-08-30)* |
| **Data Table Horizontal Encapsulation** | Mandatory `overflow-x-auto` & Min-Width Constraint on Clinical Roster & Inventory Tables | Tables with dense multi-column clinical telemetry (Patient Roster, Emergency Inventory, Audit Logs) are wrapped in `overflow-x-auto` with min-width and horizontal scroll indicators to prevent layout breaking on mobile. *(Added 2026-08-30)* |
| **Decision Audit Log Cryptographic Hash Truncation** | Priority Column Hiding on `<md` with Truncated Hashes and Full Tooltips | Retains critical tracking number, event type, and actor on mobile while truncating 64-char hashes to 8 chars with interactive tooltips and responsive disclosure. *(Added 2026-08-30)* |
| **Dark Mode Token Harmonization** | Universal Dark Tokens (`dark:bg-[#0d1424]`, `dark:border-slate-800`, `dark:text-white`) Across All Components | Eliminates hardcoded white backgrounds in `DedicatedFacilityPage.tsx`, `Sidebar.tsx`, `JurisdictionAuditLog.tsx`, `BedReservationModal.tsx`, and `PipelineSimulator.tsx`, ensuring flawless zero-glare contrast in dark clinical environments. *(Added 2026-08-30)* |
| **Modal Viewport Constraint & Sticky Composers** | `max-h-[85vh]` + Internal Scroll on Bed Reservation & Pinned Input on Unified Copilot | Enforces vertical bounds so action buttons ("Confirm Reservation") and chat input bars are permanently accessible above mobile software keyboards. *(Added 2026-08-30)* |
| **Login Gateway Accessibility & Facility Sync** | Responsive Portal Cards with Direct `selectedFacilityId` Bidirectional Autofill Binding | Increases touch target padding for mobile sign-in and ensures selecting an accredited facility immediately binds demo doctor credentials for that specific hospital. *(Added 2026-08-30)* |
| **Donor STAT Action Wrapping & Clarified Dossier Labels** | `flex-wrap gap-2` on STAT Callout Action Containers & Explicit Profile Switcher Feedback | Guarantees "Accept Mission", "Decline", and ETA badges wrap cleanly on 375px screens, and provides explicit confirmation on digital donor profile toggles. *(Added 2026-08-30)* |
| **Government Network Boundary & Shimmer States** | `max-w-7xl mx-auto` Ultrawide Boundary & Skeleton Shimmer on AI Report Generation | Prevents map and strain cards from over-stretching on 1920px ultrawide displays, and provides animated skeleton feedback during async Gemini report synthesis. *(Added 2026-08-30)* |
| **Topbar Icon Padding Optimization** | `p-1.5 sm:p-2` & `gap-1 sm:gap-2` on Mobile Viewports | Reduces dense button crowding in the Topbar on 375px viewports while preserving accessible 44px touch targets. *(Added 2026-08-30)* |
| **Issue Classifier Agent — Model & Architecture** | `gemini-3.5-flash` single LlmAgent call; deterministic keyword fallback if Gemini unavailable | Issue classification (severity, category, recommended_action) requires reasoning over free-text descriptions — cannot be purely deterministic without losing accuracy. Single Gemini call added to `POST /issues` after record creation; attaches `ai_classification` field to IssueRecord. Uses same `DEFAULT_MODEL` as all non-triage agents. Deterministic fallback ensures zero regression in offline/dev mode. *(Added 2026-08-30)* |
| **Capacity Sync — Pure Deterministic Tool (no LLM)** | Python tool, no Gemini call, exposed as `POST /capacity/sync` | Capacity reconciliation (bed count vs. patient records, blood stock vs. issued units, inventory levels) is a deterministic data-integrity check — no reasoning over ambiguous inputs is required. Per the "tools do the real work" architecture principle, this is implemented as a plain Python tool. LLM would add latency and quota cost with zero quality benefit. *(Added 2026-08-30)* |
| **Request/Donor-Matching — Deterministic Approach Locked** | Keep existing deterministic matching in `routes/requests.py`; no LLM agent added | The current `/requests` matching (append donor, flip status to `matched`) is tested, passing, and correct. Adding an LLM call this close to submission deadline adds quota risk, latency, and potential regression with zero UX benefit — the frontend label "Auto-matched by ADK" is a display string, not a backend contract. Decision confirmed and locked. *(Added 2026-08-30)* |

## ⚠️ Eligibility Compliance Checklist

- [x] **Mandatory model**: `gemini-3.5-flash` (all agents + report generator) + `gemini-3.1-pro` (Triage) — both accessed via Gemini API
- [x] **Agent framework**: Google ADK + Genkit
- [x] **Google Cloud service**: Cloud Run + Firestore
- [x] **Public repo**: README has architecture diagram + spin-up instructions
- [x] **Track**: The Taskmaster (autonomous multi-step pipeline)

## Open Items (fill before Day 1)

- [ ] **GCP project name/ID** — create it, note it in admin panel
- [ ] **Firebase project** — create in Firebase Console, download service account key
- [ ] **Demo video** — record after Day 2, link in README
